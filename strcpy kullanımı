#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

    char a[50];
    int x;
    char *ptr;
    printf("cumle giriniz: ");
    gets(a);
    x = strlen(a);

    ptr = (char *)malloc(x*sizeof(char));

    strcpy(ptr, a);
    printf("%s", ptr);
}
